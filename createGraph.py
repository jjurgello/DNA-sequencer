from igraph import *

# k = 3 # k-mer of the reads

# # pull string from a txt file
# readstr = "AAA AAB ABB BBB BBA"

# # split the reads strings into individual reads, counting the number of reads
# reads = readstr.split(' ', 1)
# num_vertices = len(reads)

# # split the reads into left/right k-1-mers and find common substrings
# edge_list = 

g = Graph()
print(g)
# g.add_vertices(num_vertices)
# g.add_edges(edge_list)